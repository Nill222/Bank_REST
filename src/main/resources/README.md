# Bank_REST API

## Сборка и запуск

### Требования:
- Java 17+
- Maven
- PostgreSQL (порт 5432, база `bank_db`, пользователь `postgres`, пароль `1234`)

### Сборка проекта
```bash
mvn clean install

